import styled from 'styled-components'
import img from './images/btn_search_banner.gif'

const DownButton = styled.button`
  background: url(${img}) no-repeat right / cover;
  border: none;
  width: 15px;
  height: 15px;
`

export default DownButton
