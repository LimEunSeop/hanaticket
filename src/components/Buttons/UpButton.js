import styled from 'styled-components'
import img from './images/btn_search_banner.gif'

const UpButton = styled.button`
  background: url(${img}) no-repeat left / cover;
  border: none;
  width: 15px;
  height: 15px;
`

export default UpButton
